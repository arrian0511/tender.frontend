/** Services */
import { ApiService } from './services/api.service';

/**
 * Array coverage of providers for the core module and testing module
 */
export const coreProviders: any[] = [
  ApiService
];

